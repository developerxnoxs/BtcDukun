version: '3.8'

services:
  # Mode Polling (Development)
  trading-bot:
    build: .
    container_name: trading-analysis-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - BOT_MODE=${BOT_MODE:-polling}
      - WEBHOOK_URL=${WEBHOOK_URL:-}
      - WEBHOOK_PORT=${WEBHOOK_PORT:-5000}
      - WEBHOOK_PATH=${WEBHOOK_PATH:-/webhook}
    ports:
      - "${WEBHOOK_PORT:-5000}:5000"
    volumes:
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('https://api.telegram.org')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Mode Webhook (Production) - Uncomment untuk menggunakan
  # trading-bot-webhook:
  #   build: .
  #   container_name: trading-analysis-bot-webhook
  #   restart: unless-stopped
  #   environment:
  #     - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
  #     - GEMINI_API_KEY=${GEMINI_API_KEY}
  #     - BOT_MODE=webhook
  #     - WEBHOOK_URL=${WEBHOOK_URL}
  #     - WEBHOOK_PORT=5000
  #     - WEBHOOK_PATH=/webhook
  #   ports:
  #     - "5000:5000"
  #   volumes:
  #     - ./logs:/app/logs
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:5000/"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 10s
